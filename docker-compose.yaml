services:
  ollama:
    image: ollama/ollama
    volumes:
      - ollama_data:/root/.ollama

  openmanus:
    build: .
    volumes:
      - ./config:/app/OpenManus/config
    depends_on:
      - ollama
    stdin_open: true
    tty: true

volumes:
  ollama_data:
